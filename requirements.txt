# Runtime dependencies (see pyproject.toml for the authoritative list).
aiohttp>=3.9        # async HTTP + WebSocket client
cryptography>=42    # RSA signing + request encryption (crypto.py)
rich>=13.0          # pretty CLI output (presentation only; library uses logging)
