# Secrets & user config
homeaccess.toml
.env

# Personal dev/deploy helpers (contain local host/IP)
deploy-ha*.ps1

# Runtime state (regenerable token + device cache)
state/
*.log

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Reverse-engineering artifacts are intentionally NOT in the repo
# (kept locally / backed up): APKs, decompiled output, captures, etc.
*.apk
*.har
captures/
